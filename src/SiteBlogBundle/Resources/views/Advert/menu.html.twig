<ul class="nav nav-pills nav-stacked">
	{% for advert in listAdverts %}
		<li>
			<a href="{{ path('site_blog_view', {'id': advert.id}) }}">
				{{ advert.title }}
			</a>
		</li>
	{% endfor %}
</ul>
